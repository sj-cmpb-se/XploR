% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CNVplot.R
\name{cov_bin}
\alias{cov_bin}
\title{Bin and Smooth Copy Number Data from Coverage File}
\usage{
cov_bin(cov, cov_binsize)
}
\arguments{
\item{cov}{Data frame or tibble. Must include columns \code{CONTIG}, \code{START}, \code{END}, and \code{CNF}.}

\item{cov_binsize}{Numeric. Bin size (in bases) for smoothing (e.g., 10000 for 10kb).}
}
\value{
A data frame with columns: \code{CONTIG}, \code{bin_start}, \code{bin_norm_id}, and smoothed CNF values.
}
\description{
Splits the coverage data by chromosome, bins into 300bp windows, and computes smoothed copy number values using a larger window size.
}
\details{
- Bins are generated every 300bp within each region.
- Smoothed copy number is computed as the median CNF within each larger (\code{cov_binsize}) window.
}
\examples{
# cov_bin(cov, cov_binsize = 10000)

}
