% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AIponprocess.R
\name{PONAIprocess}
\alias{PONAIprocess}
\title{Process Panel of Normal AI Files and Estimate Beta-binomial Dispersion based on the depth}
\usage{
PONAIprocess(
  ai_pon_file,
  aitype,
  minsnpcov = 20,
  output,
  prefix,
  maxgap = 2e+06,
  maxbinsize = 5e+06,
  minbinsize = 5e+05,
  snpnum = 30,
  gender
)
}
\arguments{
\item{ai_pon_file}{Character. Path to a text file listing PoN AI file paths (one per line).}

\item{aitype}{Character. Type of AI input file (passed to \code{ReadPonAI()}, e.g., \code{"gatk"}, \code{"dragen"}, \code{"other"}).}

\item{minsnpcov}{Integer. Minimum SNP coverage to include a site in the AI calculation. (default: 20)}

\item{output}{Character. Output directory for the processed PoN AI Rdata file.}

\item{prefix}{Character. Prefix for the output file.}

\item{maxgap}{Numeric. Maximum allowed gap between SNPs within a bin. (default: 2000000)}

\item{maxbinsize}{Numeric. Maximum allowed bin size (bp). (default: 5000000)}

\item{minbinsize}{Numeric. Minimum allowed bin size (bp). (default: 500000)}

\item{snpnum}{Integer. Target number of SNPs per bin. (default: )}
}
\value{
Invisibly returns \code{NULL}. Saves an \code{Rdata} file containing the processed PoN reference (\code{pon_ref}) and the estimated dispersion parameters (\code{theta_fit}).
}
\description{
Reads and bins allelic imbalance (AI) data from a panel of normal (PoN), summarizes bin-level statistics, and estimates beta-binomial dispersion (theta) for downstream modeling.
}
\details{
This function reads all PoN AI files, bins the AI data using \code{BinMaf()}, summarizes bin-level BAF/MAF and depth statistics, and estimates beta-binomial over-dispersion (\code{theta}) stratified by depth. The reference table and theta estimates are saved as an \code{Rdata} file for use in downstream analysis.
}
