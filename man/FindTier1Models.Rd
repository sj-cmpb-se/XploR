% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Modeling.R
\name{FindTier1Models}
\alias{FindTier1Models}
\title{Find the "Elbow" Point in a Ranked Vector (Tier 1 Model Selection)}
\usage{
FindTier1Models(values, fold)
}
\arguments{
\item{values}{Numeric vector. Typically sorted (e.g., decreasing likelihoods or scores).}

\item{fold}{Numeric. Multiplicative factor for the median drop to define a significant elbow (e.g., 1.5).}
}
\value{
Integer. The index of the first major drop ("elbow") in the \code{values} vector.
}
\description{
Identifies the first major drop ("elbow") in a sorted vector of values, using a smoothed difference approach.
}
\details{
Uses a rolling mean of negative differences to smooth noise and find the first drop exceeding \code{fold} times the median drop.
If no such drop is found, uses the geometric "elbow" method as a fallback.
}
\examples{
set.seed(1)
vals <- sort(runif(20, 0, 10), decreasing = TRUE)
FindTier1Models(vals, fold = 1.5)

}
