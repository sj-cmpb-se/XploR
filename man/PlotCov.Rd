% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CNVplot.R
\name{PlotCov}
\alias{PlotCov}
\title{Multi-panel Plot of Copy Number, BAF, Clonality, and Quality Across Chromosomes}
\usage{
PlotCov(df_cov, df_ai, call_seg, whitelist, gender, prefix, purity)
}
\arguments{
\item{df_cov}{Data frame or tibble. Smoothed coverage data, must include columns \code{seqnames}, \code{start}, \code{end}, and \code{smoothed_bin_cnf}.}

\item{df_ai}{Data frame or tibble. Smoothed AI data, must include columns \code{seqnames}, \code{bin_start}, and \code{smoothed_ai}.}

\item{call_seg}{Data frame or tibble. Segment calls, must include columns \code{seqnames}, \code{loc.start}, \code{loc.end}, \code{CN}, \code{MAF}, \code{ccf_final}, \code{FILTER}.}

\item{whitelist}{Data frame or tibble. Whitelist regions, must include columns \code{seqnames}, \code{start}, and \code{end}.}

\item{gender}{Character. Sample gender ("male" or "female").}

\item{prefix}{Character. Plot title or output prefix.}

\item{purity}{Numeric. Tumor purity (not directly used in the plot, but included for compatibility).}
}
\value{
A \code{grob} object (from \code{gridExtra::grid.arrange}) representing the combined plot.
}
\description{
Produces a summary plot with four panels: copy number, BAF, clonality, and segment quality, faceted by chromosome.
}
\examples{
# PlotCov(df_cov, df_ai, call_seg, whitelist, gender = "female", prefix = "Sample1", purity = 0.7)

}
